server:
  port: 8081
spring:
  application:
    name: api-clientes
eureka:
  client:
    registry-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka
    enabled: true